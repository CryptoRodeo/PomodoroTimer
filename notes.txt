TODO:
- Finish up the settings component so the changes are applied
  to the individual things they're linked to.

  - Make the reset button actually work

  - Fix the issue where if a user doesn't save the settings, the setting values
    return back to their default values

- Add close button to the settings component so it closes the modal

- Refactor the timer service so its easier to work with and modify

- Add more audio files

- Add auto start timer functionality to TimerService:
  - go from pomo to short break